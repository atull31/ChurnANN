# Customer Churn Prediction

A neural network model that predicts whether a bank customer is likely to churn (close their account), with an interactive Streamlit app for real-time predictions.

## Overview

This project uses an Artificial Neural Network (ANN) built with TensorFlow/Keras to perform binary classification on bank customer data. It covers the full workflow: data preprocessing, model training, evaluation, and deployment as a web app.

## Dataset

**`Churn_Modelling.csv`** — 10,000 bank customer records with the following features:

| Feature | Description |
|---|---|
| CreditScore | Customer's credit score |
| Geography | Country (France, Spain, Germany) |
| Gender | Male / Female |
| Age | Customer's age |
| Tenure | Years with the bank |
| Balance | Account balance |
| NumOfProducts | Number of bank products used |
| HasCrCard | Whether the customer has a credit card (0/1) |
| IsActiveMember | Whether the customer is an active member (0/1) |
| EstimatedSalary | Estimated annual salary |
| **Exited** | Target — whether the customer churned (0/1) |

`RowNumber`, `CustomerId`, and `Surname` are dropped during preprocessing as they carry no predictive value.

## Project Structure

```
.
├── Churn_Modelling.csv      # Raw dataset
├── experiments.ipynb        # Data preprocessing, model training & evaluation
├── prediction.ipynb         # Sample inference on a single record
├── app.py                   # Streamlit web app for live predictions
├── requirements.txt         # Python dependencies
├── model.h5                 # Trained model (generated by experiments.ipynb)
├── scaler.pkl                # Fitted StandardScaler (generated)
├── label_encoder_gender.pkl  # Fitted LabelEncoder for Gender (generated)
└── label_encoder_geo.pkl     # Fitted OneHotEncoder for Geography (generated)
```

> **Note:** `model.h5` and the `.pkl` files are produced by running `experiments.ipynb` and are not included in the repo by default — run the notebook once before using `prediction.ipynb` or `app.py`.

## Model Architecture

A simple feed-forward ANN built with Keras' `Sequential` API:

| Layer | Units | Activation |
|---|---|---|
| Dense (hidden 1) | 64 | ReLU |
| Dense (hidden 2) | 32 | ReLU |
| Dense (output) | 1 | Sigmoid |

- **Optimizer:** Adam (learning rate = 0.01)
- **Loss:** Binary Cross-Entropy
- **Callbacks:** `EarlyStopping` (patience = 10, restores best weights) and `TensorBoard` for training visualization

### Preprocessing
- `Gender` — encoded with `LabelEncoder`
- `Geography` — encoded with `OneHotEncoder`
- All features — scaled with `StandardScaler`
- Train/test split — 80/20

## Setup

```bash
# Clone the repository
git clone <repo-url>
cd <repo-folder>

# Install dependencies
pip install -r requirements.txt
```

**Dependencies:** TensorFlow, pandas, NumPy, scikit-learn, TensorBoard, Matplotlib, Streamlit

## Usage

**1. Train the model**

Run all cells in `experiments.ipynb` to preprocess the data, train the ANN, and save `model.h5` along with the encoder/scaler `.pkl` files.

**2. Run a sample prediction**

Open `prediction.ipynb` to see churn prediction on a single example record using the saved model and preprocessing objects.

**3. Launch the web app**

```bash
streamlit run app.py
```

This opens an interactive form where you can input customer details (geography, gender, age, balance, credit score, etc.) and get a real-time churn probability and prediction.

## Future Improvements
- Add model evaluation metrics (precision, recall, ROC-AUC) given the ~80/20 class imbalance in the target
- Add input validation in the Streamlit app
- Containerize the app with Docker for easier deployment
- Track experiments with MLflow alongside TensorBoard

## License

This project is open-source and available under the MIT License.
